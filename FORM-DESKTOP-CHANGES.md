# フォーム内のPC表示を修正する手順

対象：Hirameki7のフォーム36655（サイト143596）。

LPのファイルとは別に、Hirameki7側で次の2点を反映します。
スマホ表示を維持するため、改行用の通常の `<br>` は追加しません。

1. 対象フォームのCSS編集欄で、既存のCSSを残したまま `form-desktop.css` の内容を末尾に追記します。
   「その他のご相談がある場合は…」の説明欄を、PC幅ではフォームの横幅全体に広げます。
2. メール受信についての案内文のHTML編集欄で、末尾の文だけを次のように囲みます。

   ```html
   <span class="pc-mail-break">もしくは受信設定をして頂く様お願いいたします。</span>
   ```

   この案内文全体のHTMLは `form-desktop-mail.html` にあります。
   既存のメールアドレス、他の文章、生成された要素IDは維持してください。

CSSはフォーム内部の幅768px以上だけに適用します。親LPのCSSからは、別ドメインのフォーム内部を変更できません。
`form-desktop.css` をLPの `index.html` に追加しても、フォームには反映されません。

同じフォルダの `form-provider.css` は以前の別デザイン用の提案ファイルです。今回の修正には使用しません。
