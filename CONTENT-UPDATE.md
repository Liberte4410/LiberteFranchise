# Liberté FC募集LP 本文更新

2026年9月14日。ローカル実装済み。公開サイトへのアップロードは行っていません。

## 内容

- トップのデザイン・写真・文言・配置を維持し、承認された初期費用だけを「200万円〜」に変更。
- トップの無料相談・LINE相談ボタンは、PC・スマホとも静止表示。ホバー時の移動も停止。
- パンフレットをもとに、出店タイプ → 続けやすさ → 本部サポート → 費用 → 収支例 → オーナーの声 → 店舗展開 → 代表紹介 → 開業手順 → FAQ → 問い合わせの流れに再構成。
- 独立テナント型は「6坪〜・500万円前後」、併設店型は「2坪〜・200万円〜」。2坪〜は脱毛またはホワイトニングの一方だけを導入する場合であることを明記。
- 費用内訳・月額オプション・FAQは開閉式。要点を先に読み、詳細を必要に応じて確認できる構成。
- 年間売上5,726,000円、経費2,701,400円、利益3,024,600円、利益率52.8%はPDF掲載例として表示。通常運営時間・収支・費用の条件を併記。
- 本文はPCで概ね18〜20px、スマホで17〜18px。見出しを意味のまとまりで改行し、スマホで語尾だけが残る箇所を調整。

## 変更・追加ファイル

| ファイル | 用途 |
| --- | --- |
| `index.html` | 本文、ナビゲーションのリンク名、検索用説明文、トップの金額 |
| `content.css` | 新しい本文のデザイン、レスポンシブ表示、相談ボタンのアニメーション停止 |
| `images/lp-icons.svg` | 店舗・予約・集客などの説明用アイコン |
| `images/illustration-tenant.svg` | 独立テナント型の店舗イラスト |
| `images/illustration-annex.svg` | 既存店舗の一角に併設するイラスト |
| `images/illustration-owner.svg` | オーナーの清掃・補充業務イラスト |
| `images/illustration-support.svg` | 本部のサポート業務イラスト |
| `images/brochure-tenant.jpg` | PDF 3ページから取り出した単独店舗の写真 |
| `images/brochure-annex.jpg` | PDF 3ページから取り出したホワイトニング設備の写真 |
| `form-mobile-width-fix.css` | 提供元で適用するフォーム幅の補正。LPには読み込んでいません |

`style.css` と `script.js` は今回変更していません。既存のフォーム埋め込み、LINE URL、フッターを保持しています。
トップ写真は `hero-self-care.webp`、ロゴは `logo.svg` を継続使用。その他の既存画像は `voice-k.svg`、`voice-y.svg`、`japan-map.png`、`president.jpeg`、`line-qr.jpeg` です。

## 表示・操作確認

- Edgeの画面幅320・375・390・430・768・1024・1280・1440・1920pxで本文を確認。最終調整後は320・375・768・1440pxを再確認。
- 新しい本文の横はみ出し・画像の読み込み失敗・リンク先不在がないことを確認。
- 費用内訳とFAQを展開した状態も320・375・768・1440pxで幅を確認。
- 375・1440pxで、メニューとアンカー、費用詳細・FAQのマウス／キーボード操作、相談ボタンの静止表示を確認。
- 既存の問い合わせフォームの読み込みを確認。問い合わせは送信していません。
- トップのHTMLは金額以外が元と一致。フォーム埋め込みとフッターのHTMLも元と一致。
- 実機のiPhone Safariによる検証ではなく、Edgeで各幅を再現した検証です。

## 外部フォームに残る修正

Hirameki7のフォームID **36655** は別ドメインのiframeです。スマホでは内部のグリッドの左マージンとpaddingが重なり、市区町村・番地・建物名の右端が12pxはみ出しています。

LPのCSSからこの内部レイアウトは変更できません。**Hirameki7側の当該フォームのカスタムCSS末尾**に `form-mobile-width-fix.css` の内容を追加する必要があります。既存CSS全体を置き換えず、追記してください。

補正はテスト用ブラウザ内でのみ検証しており、提供元への保存はしていません。スマホ用グリッドの余分な幅を正す内容で、送信先・必須設定・入力内容には触れません。以前の別案件用 `form-provider.css` や `form-desktop.css` をLPに読み込む必要はありません。

補正の一時適用後は画面幅320・375・390・430pxで全入力欄がiframe内に収まり、内部の横スクロールも解消することを確認しました。1440pxのPC表示は補正の前後で同一でした。

## スクリーンショット

- `review/desktop-store-types.png`：PCの2つの出店スタイル
- `review/mobile-tenant.png`：スマホの独立テナント型
- `review/mobile-annex.png`：スマホの併設店型
- `review/hero-1440.png`、`review/hero-375.png`：トップ
- `review/form-mobile-width-fix-preview.png`：提供元用CSSの一時適用プレビュー（未保存）

## 資料

- 内容・数値・店舗写真：`C:/Users/iwamoto/Downloads/Liberte_展示会パンフ_6P_修正版_8_アイコン差替.pdf`（全6ページ確認）。
- 読みやすさの参考：[HAKU FC募集サイト](https://fc.whitening-haku.com/)。短い見出しと図解の使い方を参考にし、本文・写真・医療表現は転用していません。
- 14店舗決定の情報・地図・会社所在地・フォーム・LINE URL：変更前のLibertéサイト。

変更前のローカルファイルは `.preview/brochure-before-index.html`、`.preview/brochure-before-style.css`、`.preview/brochure-before-script.js` に保存しています。
